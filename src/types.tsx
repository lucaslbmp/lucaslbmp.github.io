export type ButtonStyle = "primary" | "ghost" | "danger";

export type ExperienceDetails = {
  role: string;
  company: string;
  description: string;
  skills: string[];
  startDate: Date;
  endDate: Date;
};
